const { expect } = require("chai");
const { QueryBuilder } = require("../index");

describe("Query Builder", () => {
  const queryBuilder = new QueryBuilder();
  queryBuilder.
  describe("addStage", () => {
    it("Add one stage", () => {});
    it("Add multiple stages", () => {});
    it("add a invalid stage");
  });

  describe("addPipeline", () => {
    it("Add a pipeline", () => {});
    it("Add a invalid pipeline", () => {});
  });

  describe("addStageAndPersist", () => {
    it("Add a pipeline", () => {});
  });
});
