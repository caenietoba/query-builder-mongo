const errorMessages = {
  notPlainObject: "Stage must be a js plain object",
  notMongoStage: "Stage is not a valid mongo aggregate stage",
  pathNotFound: "The path does not exists in the stages Store",
};

module.exports = {
  errorMessages,
};
